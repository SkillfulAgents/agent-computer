{
  "name": "@skillful-agents/agent-computer",
  "version": "0.0.2",
  "description": "Agent Computer — cross-platform desktop automation CLI for AI agents",
  "type": "module",
  "bin": {
    "agent-computer": "./dist/bin/ac.js"
  },
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/src/index.js",
      "types": "./dist/src/index.d.ts"
    }
  },
  "files": [
    "dist/",
    "bin/ac-core-*",
    "scripts/",
    "schema/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "build:swift": "cd native/macos && swift build -c release",
    "build:dotnet": "cd native/windows && dotnet build ACCore/ACCore.csproj",
    "build:all": "npm run build && (node -e \"process.exit(process.platform==='win32'?0:1)\" && npm run build:dotnet || npm run build:swift)",
    "postinstall": "node scripts/postinstall.js || true",
    "prepublishOnly": "npm run build",
    "test": "vitest run test/contract/ test/unit/",
    "test:contract": "vitest run test/contract/",
    "test:unit": "vitest run test/unit/",
    "test:integration": "vitest run test/integration/",
    "test:functional": "vitest run test/functional/",
    "test:e2e": "vitest run test/e2e/ --timeout 120000",
    "test:all": "vitest run",
    "test:watch": "vitest watch test/unit/ test/contract/",
    "typecheck": "tsc --noEmit",
    "agent-computer": "tsx bin/ac.ts"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "ajv": "^8.17.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20",
    "os": "darwin || win32"
  },
  "os": [
    "darwin",
    "win32"
  ],
  "cpu": [
    "arm64",
    "x64"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/datawizz/ac"
  }
}
